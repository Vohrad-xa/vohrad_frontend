diff --git a/ios/Shared/RCTMenuItem.swift b/ios/Shared/RCTMenuItem.swift
index bb6bb2b7ad56135089f267587c974b166760539d..2650dc596f1c05017b0b65d72b9cf76caba16d34 100644
--- a/ios/Shared/RCTMenuItem.swift
+++ b/ios/Shared/RCTMenuItem.swift
@@ -14,6 +14,10 @@ class RCTMenuAction {
     var title: String;
     var subtitle: String?
     var displayInline: Bool
+    var menuOptions: NSDictionary?
+    var menuSingleSelection: Bool = false
+    var menuDestructive: Bool = false
+    var menuDisplayInlineOverride: Bool?
     var image: UIImage?
     var attributes: UIAction.Attributes = []
     var state: UIAction.State = .off
@@ -53,6 +57,19 @@ class RCTMenuAction {
             self.displayInline = false;
         }
 
+        if let menuOptions = details["menuOptions"] as? NSDictionary {
+            self.menuOptions = menuOptions
+            if let singleSelection = menuOptions["singleSelection"] as? Bool {
+                self.menuSingleSelection = singleSelection
+            }
+            if let destructive = menuOptions["destructive"] as? Bool {
+                self.menuDestructive = destructive
+            }
+            if let displayInlineOverride = menuOptions["displayInline"] as? Bool {
+                self.menuDisplayInlineOverride = displayInlineOverride
+            }
+        }
+
         if let attributes = details["attributes"] as? NSDictionary {
             if (attributes["destructive"] as? Bool) == true {
                 self.attributes.update(with: .destructive)
@@ -102,12 +119,22 @@ class RCTMenuAction {
             subactions.forEach { subaction in
                 subMenuActions.append(subaction.createUIMenuElement(handler))
             }
-            var menu: UIMenu;
-            if self.displayInline {
-                menu = UIMenu(title: title, image: image, options: .displayInline, children: subMenuActions)
-            } else {
-                menu = UIMenu(title: title, image: image, children: subMenuActions)
+            var options: UIMenu.Options = []
+
+            let shouldDisplayInline = self.menuDisplayInlineOverride ?? self.displayInline
+            if shouldDisplayInline {
+                options.insert(.displayInline)
+            }
+            if #available(iOS 14.0, *) {
+                if self.menuSingleSelection {
+                    options.insert(.singleSelection)
+                }
             }
+            if self.menuDestructive {
+                options.insert(.destructive)
+            }
+
+            var menu = UIMenu(title: title, image: image, options: options, children: subMenuActions)
 
             if #available(iOS 16.0, *) {
                 if(preferredElementSizeString == "small") {
diff --git a/lib/module/NativeModuleSpecs/UIMenuNativeComponent.ts b/lib/module/NativeModuleSpecs/UIMenuNativeComponent.ts
index e6509355275596c451f9d082223cc16bfe504e1a..5fe8af4dc973b63a7005d100c53eaec3d9d9caee 100644
--- a/lib/module/NativeModuleSpecs/UIMenuNativeComponent.ts
+++ b/lib/module/NativeModuleSpecs/UIMenuNativeComponent.ts
@@ -22,10 +22,17 @@ type SubAction = {
 	image?: string;
 	imageColor?: Int32;
 	displayInline?: boolean;
+	preferredElementSize?: string;
+	menuOptions?: {
+		singleSelection?: boolean;
+		destructive?: boolean;
+		displayInline?: boolean;
+	};
 	attributes?: {
 		destructive?: boolean;
 		disabled?: boolean;
 		hidden?: boolean;
+		keepsMenuPresented?: boolean;
 	};
 };
 type MenuAction = {
@@ -37,10 +44,17 @@ type MenuAction = {
 	image?: string;
 	imageColor?: Int32;
 	displayInline?: boolean;
+	preferredElementSize?: string;
+	menuOptions?: {
+		singleSelection?: boolean;
+		destructive?: boolean;
+		displayInline?: boolean;
+	};
 	attributes?: {
 		destructive?: boolean;
 		disabled?: boolean;
 		hidden?: boolean;
+		keepsMenuPresented?: boolean;
 	};
 	subactions?: Array<SubAction>;
 };
diff --git a/lib/typescript/src/NativeModuleSpecs/UIMenuNativeComponent.d.ts b/lib/typescript/src/NativeModuleSpecs/UIMenuNativeComponent.d.ts
index 24ee7fdcd9f81cfe2c813cfca9d4e276b846fb7b..85e521a9464b84da7d19f38f5af045c979b45065 100644
--- a/lib/typescript/src/NativeModuleSpecs/UIMenuNativeComponent.d.ts
+++ b/lib/typescript/src/NativeModuleSpecs/UIMenuNativeComponent.d.ts
@@ -9,10 +9,17 @@ type SubAction = {
     image?: string;
     imageColor?: Int32;
     displayInline?: boolean;
+    preferredElementSize?: string;
+    menuOptions?: {
+        singleSelection?: boolean;
+        destructive?: boolean;
+        displayInline?: boolean;
+    };
     attributes?: {
         destructive?: boolean;
         disabled?: boolean;
         hidden?: boolean;
+        keepsMenuPresented?: boolean;
     };
 };
 type MenuAction = {
@@ -24,10 +31,17 @@ type MenuAction = {
     image?: string;
     imageColor?: Int32;
     displayInline?: boolean;
+    preferredElementSize?: string;
+    menuOptions?: {
+        singleSelection?: boolean;
+        destructive?: boolean;
+        displayInline?: boolean;
+    };
     attributes?: {
         destructive?: boolean;
         disabled?: boolean;
         hidden?: boolean;
+        keepsMenuPresented?: boolean;
     };
     subactions?: Array<SubAction>;
 };
diff --git a/lib/typescript/src/types.d.ts b/lib/typescript/src/types.d.ts
index eac7940ab7eb6a69ac329848c9f3eeffdcbaf43d..de0e7b4f0a4eb5ff5654847b9a09a6479613d6f0 100644
--- a/lib/typescript/src/types.d.ts
+++ b/lib/typescript/src/types.d.ts
@@ -91,6 +91,22 @@ export type MenuAction = {
      * Whether subactions should be inline (separated by divider) or nested (sub menu)
      */
     displayInline?: boolean;
+    /**
+     * Options applied when rendering this action as a UIMenu (iOS).
+     */
+    menuOptions?: {
+        displayInline?: boolean;
+        /**
+         * @platform iOS
+         * Forces single selection for children when set.
+         */
+        singleSelection?: boolean;
+        /**
+         * @platform iOS
+         * Marks the menu as destructive.
+         */
+        destructive?: boolean;
+    };
     /**
      * (iOS 16+ only)
      * The preferred size of this menu's child elements.
diff --git a/src/NativeModuleSpecs/UIMenuNativeComponent.ts b/src/NativeModuleSpecs/UIMenuNativeComponent.ts
index e6509355275596c451f9d082223cc16bfe504e1a..5fe8af4dc973b63a7005d100c53eaec3d9d9caee 100644
--- a/src/NativeModuleSpecs/UIMenuNativeComponent.ts
+++ b/src/NativeModuleSpecs/UIMenuNativeComponent.ts
@@ -22,10 +22,17 @@ type SubAction = {
 	image?: string;
 	imageColor?: Int32;
 	displayInline?: boolean;
+	preferredElementSize?: string;
+	menuOptions?: {
+		singleSelection?: boolean;
+		destructive?: boolean;
+		displayInline?: boolean;
+	};
 	attributes?: {
 		destructive?: boolean;
 		disabled?: boolean;
 		hidden?: boolean;
+		keepsMenuPresented?: boolean;
 	};
 };
 type MenuAction = {
@@ -37,10 +44,17 @@ type MenuAction = {
 	image?: string;
 	imageColor?: Int32;
 	displayInline?: boolean;
+	preferredElementSize?: string;
+	menuOptions?: {
+		singleSelection?: boolean;
+		destructive?: boolean;
+		displayInline?: boolean;
+	};
 	attributes?: {
 		destructive?: boolean;
 		disabled?: boolean;
 		hidden?: boolean;
+		keepsMenuPresented?: boolean;
 	};
 	subactions?: Array<SubAction>;
 };
diff --git a/src/types.ts b/src/types.ts
index 9c323d9c510e7359c83d4d6633affde673570632..64fddfea68e0686a6731a1176c71043c9bce31cc 100644
--- a/src/types.ts
+++ b/src/types.ts
@@ -41,6 +41,20 @@ type MenuAttributes = {
  */
 type MenuState = "off" | "on" | "mixed";
 
+type MenuOptions = {
+	displayInline?: boolean;
+	/**
+	 * @platform iOS
+	 * Forces single selection for children when set.
+	 */
+	singleSelection?: boolean;
+	/**
+	 * @platform iOS
+	 * Marks the menu as destructive.
+	 */
+	destructive?: boolean;
+};
+
 export type MenuAction = {
 	/**
 	 * Identifier of the menu action.
@@ -100,6 +114,10 @@ export type MenuAction = {
 	 * Whether subactions should be inline (separated by divider) or nested (sub menu)
 	 */
 	displayInline?: boolean;
+	/**
+	 * Options applied when rendering this action as a UIMenu (iOS).
+	 */
+	menuOptions?: MenuOptions;
 	/**
 	 * (iOS 16+ only)
 	 * The preferred size of this menu's child elements.
